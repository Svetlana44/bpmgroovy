# Контрактное тестирование и Wiremock - Практические примеры

## 📦 Установка зависимостей

Зависимости уже добавлены в `build.gradle`. Если нужно обновить:

```gradle
// Wiremock
testImplementation 'com.github.tomakehurst:wiremock-jre8:2.35.0'

// Pact
testImplementation 'au.com.dius.pact.consumer:junit5:4.6.2'
testImplementation 'au.com.dius.pact.provider:junit5:4.6.2'
```

## 🔧 Настройка

### Wiremock
Wiremock запускается автоматически в тестах. Порт по умолчанию: 8089

### Pact
Pact создает контракты автоматически при запуске тестов. Контракты сохраняются в `target/pacts/` (Maven) или `build/pacts/` (Gradle).

## 📝 Примеры использования

### Wiremock

**Основные сценарии:**

1. **Простая заглушка GET запроса**
   - См. `testSimpleGetStub()` в `WiremockExampleTest.java`

2. **POST запрос с проверкой тела**
   - См. `testPostStubWithBodyMatching()`

3. **Query параметры**
   - См. `testStubWithQueryParameters()`

4. **Обработка ошибок**
   - См. `testErrorStub()`

5. **Задержки (таймауты)**
   - См. `testStubWithDelay()`

6. **Проверка заголовков**
   - См. `testStubWithHeaderMatching()`

7. **Верификация запросов**
   - См. `testRequestVerification()`

### Pact

**Основные концепции:**

1. **Consumer** - сервис, который использует API
2. **Provider** - сервис, который предоставляет API
3. **Contract** - описание ожидаемого взаимодействия

**Примеры:**

1. **GET запрос с контрактом**
   - См. `testGetUser()` в `PactContractTestExample.java`

2. **POST запрос с контрактом**
   - См. `testCreateUser()`

## 🚀 Запуск тестов

```bash
# Запуск всех тестов контрактного тестирования
./gradlew test --tests contract.*

# Запуск только Wiremock тестов
./gradlew test --tests contract.WiremockExampleTest

# Запуск только Pact тестов
./gradlew test --tests contract.PactContractTestExample
```

## 📚 Дополнительные ресурсы

### Wiremock
- Официальная документация: http://wiremock.org/docs/
- Примеры: https://github.com/wiremock/wiremock

### Pact
- Официальная документация: https://docs.pact.io/
- Pact Broker: https://docs.pact.io/pact_broker
- Примеры: https://github.com/pact-foundation/pact-jvm

## 💡 Практические советы

### Wiremock
1. Используйте для изоляции тестов от внешних сервисов
2. Создавайте заглушки для различных сценариев (успех, ошибки)
3. Используйте верификацию для проверки, что запросы были выполнены
4. Сохраняйте заглушки в файлы для переиспользования

### Pact
1. Начинайте с Consumer тестов
2. Используйте Provider тесты для верификации контрактов
3. Храните контракты в Pact Broker для команды
4. Интегрируйте в CI/CD для автоматической проверки

## ⚠️ Важные замечания

1. **Wiremock** - для мокирования HTTP сервисов в тестах
2. **Pact** - для контрактного тестирования между сервисами
3. Оба инструмента можно использовать вместе
4. Pact создает контракты, которые можно использовать для верификации Provider
