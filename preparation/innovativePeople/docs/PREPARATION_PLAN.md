# План подготовки к вакансии QA Automation (Java/Kotlin) + Контрактное тестирование

## 📋 Анализ требований

### ✅ Что уже есть в проекте:
- RestAssured для API тестирования
- Selenium/Selenide для UI тестирования
- JUnit 5
- Работа с БД (PostgreSQL, MSSQL)
- JSON Schema валидация
- Allure отчеты

### ❌ Что нужно добавить/изучить:
- **Контрактное тестирование** (Pact или Spring Cloud Contract)
- **Wiremock** для заглушек
- **Playwright** (альтернатива Selenium)
- **Kafka** (брокер сообщений)
- **MongoDB** (нереляционная БД)
- **Kibana** (работа с логами)

---

## 🎯 Приоритетный план подготовки (по важности)

### 1. КОНТРАКТНОЕ ТЕСТИРОВАНИЕ (Критично!)

**Что это:** Проверка соответствия API реальной реализации документации (контракту). Контракт — это соглашение о том, как сервисы должны взаимодействовать.

**⚠️ ВАЖНО: На собеседовании могут спросить про любой из подходов!**

#### Подход 1: Валидация соответствия Swagger/OpenAPI (НАИБОЛЕЕ РАСПРОСТРАНЕННЫЙ!)

**Что это:** Проверка, что реальный API соответствует документации Swagger/OpenAPI.

**Концепция:**
- Swagger/OpenAPI — это спецификация (контракт), описывающая API
- Контрактное тестирование = проверка, что реальные запросы/ответы соответствуют этой спецификации
- Если API отклоняется от документации — тест падает

**Что проверять:**
1. Структура запросов соответствует спецификации
2. Структура ответов соответствует спецификации
3. Обязательные поля присутствуют
4. Типы данных корректны
5. Статус коды соответствуют документации
6. Headers соответствуют спецификации

**Инструменты:**
- **RestAssured + OpenAPI Validator** — валидация ответов против OpenAPI схемы
- **Schemathesis** — автоматическая генерация тестов из OpenAPI
- **Dredd** — валидация API против документации
- **Postman** — валидация против OpenAPI схемы
- **JSON Schema валидация** — проверка структуры JSON

**Пример с RestAssured:**
```java
// Валидация ответа против JSON Schema (из OpenAPI)
given()
    .header("Authorization", "Bearer " + token)
.when()
    .get("/api/users/123")
.then()
    .statusCode(200)
    .body(matchesJsonSchemaInClasspath("user-schema.json")); // Валидация схемы

// Или валидация обязательных полей
.then()
    .statusCode(200)
    .body("id", notNullValue())
    .body("name", notNullValue())
    .body("email", matchesRegex("^[A-Za-z0-9+_.-]+@(.+)$"));
```

**Практика:**
- Получить Swagger/OpenAPI спецификацию API
- Написать тесты, которые валидируют ответы против этой спецификации
- Использовать JSON Schema валидацию
- Проверять обязательные поля, типы данных
- Сравнить реальные запросы/ответы с документацией

**Ресурсы:**
- https://swagger.io/specification/ - OpenAPI спецификация
- https://www.baeldung.com/rest-assured-json-schema - Валидация JSON Schema в RestAssured
- https://github.com/OpenAPITools/openapi-generator - Генерация клиентов из OpenAPI
- https://schemathesis.readthedocs.io/ - Schemathesis для автоматической генерации тестов

---

#### Подход 2: Pact / Consumer-Driven Contracts (ПРОДВИНУТЫЙ ПОДХОД)

**Что это:** Более продвинутый подход, где контракты создаются потребителем сервиса (consumer), который определяет, как он ожидает взаимодействовать с провайдером (provider).

**Концепция:**
- Consumer (потребитель) создает контракт, описывающий ожидаемое взаимодействие
- Provider (провайдер) должен соответствовать этому контракту
- Контракты хранятся в Pact Broker
- При изменении контракта провайдер получает уведомление

**Инструменты:**
- **Pact** (Consumer-Driven Contracts) - самый популярный
- **Spring Cloud Contract** (для Spring приложений)

**Что изучить:**
1. Концепция Consumer-Driven Contracts
2. Pact Broker
3. Написание контрактов (Provider/Consumer)
4. Верификация контрактов в CI/CD

**Практика:**
- Создать простой пример с Pact
- Написать контракт для существующего API
- Настроить верификацию

**Ресурсы:**
- https://docs.pact.io/ - Официальная документация Pact
- https://docs.pact.io/implementation_guides/workshops - Официальные воркшопы для Java
- https://docs.pactflow.io/docs/tutorials - Интерактивные туториалы PactFlow
- https://habr.com/ru/companies/kuper/articles/845964/ - Статья на русском: "Контрактные тесты с Pact"
- https://habr.com/ru/companies/testit-tms/articles/570544/ - Статья: "Spring Cloud Contract. Что такое контрактное тестирование"
- https://www.udemy.com/course/microservices-contract-testing-with-pact-java-2022/ - Курс на Udemy (платный)

---

**Какой подход изучать?**

**Для собеседования рекомендуется знать ОБА подхода:**

1. **Начать с подхода 1 (Swagger/OpenAPI)** — это более распространенный и понятный подход
   - Большинство компаний используют именно этот подход
   - Проще объяснить на собеседовании
   - Легче реализовать
   - **Это то, о чем вы спрашивали!**

2. **Знать про подход 2 (Pact)** — для демонстрации глубоких знаний
   - Показать, что знаете продвинутые техники
   - Полезно для микросервисной архитектуры
   - Может быть плюсом, если компания использует Pact

**На вопрос "Что такое контрактное тестирование?" можно ответить:**
"Контрактное тестирование — это проверка соответствия API документации (например, Swagger/OpenAPI) реальной реализации. Мы проверяем, что запросы и ответы соответствуют спецификации. Также существует более продвинутый подход — Consumer-Driven Contracts с использованием Pact, где контракты создаются потребителем сервиса."

---

## 🔵 Pact и Pact Broker - подробное руководство

### Что такое Pact?

**Pact** — это инструмент для контрактного тестирования, который реализует подход Consumer-Driven Contracts (CDC). 

**Основная идея:**
- **Consumer (потребитель)** — сервис, который использует API другого сервиса
- **Provider (провайдер)** — сервис, который предоставляет API
- Consumer создает контракт, описывающий, как он ожидает взаимодействовать с Provider
- Provider должен соответствовать этому контракту

**Преимущества:**
- Раннее обнаружение breaking changes
- Быстрые тесты (не требуют запуска всех сервисов)
- Явное описание ожиданий между сервисами
- Автоматическая проверка совместимости

---

### Что такое Pact Broker?

**Pact Broker** — это веб-приложение для хранения и управления контрактами Pact между Consumer и Provider сервисами.

**Основные функции:**

1. **Хранение контрактов:**
   - Хранит все версии контрактов
   - История изменений контрактов
   - Версионирование контрактов

2. **Верификация контрактов:**
   - Provider может проверить, соответствует ли он контрактам всех Consumer
   - Автоматическая верификация при деплое Provider
   - Отчеты о верификации

3. **Управление зависимостями:**
   - Показывает, какие Consumer зависят от какого Provider
   - Отслеживание совместимости версий
   - Матрица совместимости

4. **Уведомления:**
   - Уведомления при изменении контрактов
   - Интеграция с CI/CD
   - Webhooks для автоматизации

5. **Визуализация:**
   - Веб-интерфейс для просмотра контрактов
   - Граф зависимостей между сервисами
   - Статус верификации контрактов

**Альтернативы Pact Broker:**
- **PactFlow** — коммерческая версия с дополнительными возможностями
- **Локальное хранение** — контракты в файловой системе (для простых случаев)

---

### Как работает Pact Broker?

**Процесс работы:**

1. **Consumer создает контракт:**
   ```java
   // Consumer тест создает контракт
   @Test
   public void testUserService() {
       // Определяем ожидаемое взаимодействие
       pactConsumer
           .given("user exists")
           .uponReceiving("a request for user")
           .path("/api/users/123")
           .method("GET")
           .willRespondWith()
           .status(200)
           .body(new PactDslJsonBody()
               .integerType("id", 123)
               .stringType("name", "John Doe")
               .stringType("email", "john@example.com"))
           .toPact();
   }
   ```

2. **Контракт публикуется в Pact Broker:**
   ```bash
   # Consumer публикует контракт в Broker
   ./gradlew pactPublish
   # или
   curl -X PUT http://pact-broker/pacts/provider/UserService/consumer/OrderService/version/1.0.0 \
     -H "Content-Type: application/json" \
     -d @pact-file.json
   ```

3. **Provider верифицирует контракт:**
   ```java
   // Provider тест верифицирует контракт
   @Test
   public void verifyPacts() {
       PactVerificationResult result = pactProvider
           .verifyPactsFromPactBroker("http://pact-broker")
           .provider("UserService")
           .consumer("OrderService")
           .verify();
   }
   ```

4. **Результаты верификации публикуются обратно в Broker:**
   - Broker показывает статус верификации
   - Если верификация не прошла — контракт помечается как невалидный
   - Consumer получает уведомление о проблеме

---

### Примеры использования Pact

#### Пример 1: Consumer тест (создание контракта)

```java
import au.com.dius.pact.consumer.Pact;
import au.com.dius.pact.consumer.PactProviderRuleMk2;
import au.com.dius.pact.consumer.PactVerification;
import au.com.dius.pact.consumer.dsl.PactDslJsonBody;
import au.com.dius.pact.model.RequestResponsePact;

public class UserServiceConsumerTest {
    
    @Rule
    public PactProviderRuleMk2 mockProvider = new PactProviderRuleMk2("UserService", "localhost", 8080, this);
    
    // Создаем контракт
    @Pact(consumer = "OrderService")
    public RequestResponsePact createPact(PactDslWithProvider builder) {
        return builder
            .given("user exists with id 123")
            .uponReceiving("a request for user by id")
                .path("/api/users/123")
                .method("GET")
                .headers("Authorization", "Bearer token123")
            .willRespondWith()
                .status(200)
                .headers(Map.of("Content-Type", "application/json"))
                .body(new PactDslJsonBody()
                    .integerType("id", 123)
                    .stringType("name", "John Doe")
                    .stringType("email", "john@example.com")
                    .stringType("status", "active"))
            .toPact();
    }
    
    // Тест, который использует контракт
    @Test
    @PactVerification("UserService")
    public void testGetUser() {
        // Выполняем запрос к мок-серверу (созданному Pact)
        Response response = given()
            .baseUri(mockProvider.getUrl())
            .header("Authorization", "Bearer token123")
        .when()
            .get("/api/users/123")
        .then()
            .statusCode(200)
            .body("id", equalTo(123))
            .body("name", equalTo("John Doe"))
            .extract().response();
        
        // Контракт автоматически проверяется
    }
}
```

#### Пример 2: Публикация контракта в Pact Broker

```java
// build.gradle
plugins {
    id 'au.com.dius.pact' version '4.3.0'
}

dependencies {
    testImplementation 'au.com.dius.pact.consumer:junit5:4.3.0'
}

pact {
    publish {
        pactDirectory = 'build/pacts'
        pactBrokerUrl = 'http://localhost:9292'
        version = project.version
    }
}
```

```bash
# Публикация контракта
./gradlew pactPublish

# Или через Maven
mvn pact:publish
```

#### Пример 3: Provider тест (верификация контракта)

```java
import au.com.dius.pact.provider.junit5.PactVerificationContext;
import au.com.dius.pact.provider.junit5.PactVerificationInvocationContextProvider;
import au.com.dius.pact.provider.junitsupport.Provider;
import au.com.dius.pact.provider.junitsupport.State;
import au.com.dius.pact.provider.junitsupport.loader.PactBroker;

@Provider("UserService")
@PactBroker(url = "http://localhost:9292")
public class UserServiceProviderTest {
    
    @TestTemplate
    @ExtendWith(PactVerificationInvocationContextProvider.class)
    void pactVerificationTestTemplate(PactVerificationContext context) {
        context.verifyInteraction();
    }
    
    @BeforeEach
    void before(PactVerificationContext context) {
        // Настройка тестового окружения
        context.setTarget(new HttpTestTarget("localhost", 8080));
    }
    
    // Подготовка состояния для теста
    @State("user exists with id 123")
    public void userExists() {
        // Подготовка данных в БД или мок-сервисе
        // Например, создание пользователя с id 123
        userRepository.save(new User(123, "John Doe", "john@example.com", "active"));
    }
}
```

#### Пример 4: Настройка Pact Broker

**Локальный запуск Pact Broker (Docker):**

```yaml
# docker-compose.yml
version: '3'
services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_USER: pact
      POSTGRES_PASSWORD: pact
      POSTGRES_DB: pact
  
  pact-broker:
    image: pactfoundation/pact-broker:latest
    ports:
      - "9292:9292"
    environment:
      PACT_BROKER_DATABASE_URL: postgres://pact:pact@postgres/pact
      PACT_BROKER_BASIC_AUTH_USERNAME: admin
      PACT_BROKER_BASIC_AUTH_PASSWORD: admin
    depends_on:
      - postgres
```

```bash
# Запуск Pact Broker
docker-compose up -d

# Доступ к веб-интерфейсу
# http://localhost:9292
```

#### Пример 5: Интеграция с CI/CD

```yaml
# .gitlab-ci.yml
stages:
  - test
  - publish
  - verify

# Consumer: создание и публикация контракта
consumer_test:
  stage: test
  script:
    - ./gradlew test
    - ./gradlew pactPublish
  only:
    - merge_requests

# Provider: верификация контрактов
provider_verify:
  stage: verify
  script:
    - ./gradlew pactVerify
  only:
    - main
```

#### Пример 6: Работа с версиями контрактов

```java
// Consumer публикует контракт с версией
pact {
    publish {
        pactBrokerUrl = 'http://localhost:9292'
        version = '1.2.3' // Версия Consumer
    }
}

// Provider верифицирует контракты определенных версий
@PactBroker(
    url = "http://localhost:9292",
    consumerVersionSelectors = {
        @VersionSelector(tag = "main"), // Верифицировать контракты с тегом "main"
        @VersionSelector(latest = true) // Или последние версии
    }
)
```

---

### Ключевые концепции Pact

**1. Consumer-Driven Contracts (CDC):**
- Контракты создаются Consumer (потребителем)
- Consumer определяет, что ему нужно от Provider
- Provider должен соответствовать контрактам всех Consumer

**2. Pact файлы:**
- JSON файлы, описывающие взаимодействие
- Создаются автоматически при запуске Consumer тестов
- Хранятся в Pact Broker или файловой системе

**3. Верификация:**
- Provider запускает свои тесты против контрактов
- Проверяется, что Provider соответствует контрактам
- Результаты публикуются в Broker

**4. Матрица совместимости:**
- Показывает, какие версии Consumer совместимы с какими версиями Provider
- Помогает отслеживать breaking changes

**5. Can-I-Deploy:**
- Утилита для проверки, можно ли деплоить новую версию
- Проверяет, что все контракты верифицированы
- Используется в CI/CD pipeline

```bash
# Проверка перед деплоем
docker run --rm \
  -v ${PWD}:${PWD} \
  -w ${PWD} \
  pactfoundation/pact-cli:latest \
  broker can-i-deploy \
  --pact-broker-base-url http://pact-broker \
  --pacticipant UserService \
  --version 1.2.3 \
  --to main
```

---

### Преимущества использования Pact Broker

1. **Централизованное хранение:**
   - Все контракты в одном месте
   - Легко найти нужный контракт
   - История изменений

2. **Автоматизация:**
   - Автоматическая верификация при деплое
   - Интеграция с CI/CD
   - Уведомления о проблемах

3. **Визуализация:**
   - Веб-интерфейс для просмотра контрактов
   - Граф зависимостей
   - Статус верификации

4. **Управление версиями:**
   - Отслеживание версий контрактов
   - Матрица совместимости
   - Теги для версий

5. **Коллаборация:**
   - Команды видят контракты друг друга
   - Понятно, кто от кого зависит
   - Раннее обнаружение проблем

---

### Когда использовать Pact?

**Используйте Pact, когда:**
- Микросервисная архитектура
- Несколько Consumer для одного Provider
- Нужна ранняя обратная связь о breaking changes
- Команды работают независимо
- Нужна автоматизация проверки совместимости

**Не используйте Pact, когда:**
- Монолитное приложение
- Один Consumer для одного Provider
- Простые API без сложных зависимостей
- Нет необходимости в автоматизации

---

### Ресурсы для изучения Pact и Pact Broker

- **Официальная документация Pact:** https://docs.pact.io/
- **Pact Broker документация:** https://docs.pact.io/pact_broker
- **Pact JVM (Java):** https://github.com/DiUS/pact-jvm
- **Примеры Pact:** https://github.com/DiUS/pact-jvm/tree/master/consumer/junit5
- **PactFlow (коммерческая версия):** https://pactflow.io/
- **Статья на Habr:** https://habr.com/ru/companies/kuper/articles/845964/

---

### 2. WIREMOCK (Желательно, но важно)

**Что это:** Мокирование HTTP сервисов для изоляции тестов.

**Что изучить:**
1. Настройка Wiremock сервера
2. Создание стабов (stubs)
3. Запись/воспроизведение запросов
4. Динамические ответы
5. Интеграция с тестами

**Практика:**
- Настроить Wiremock в проекте
- Создать заглушки для внешних API
- Использовать в API тестах

**Ресурсы:**
- http://wiremock.org/docs/
- https://www.baeldung.com/introduction-to-wiremock

---

### 3. REST API ТЕСТИРОВАНИЕ (Уверенные знания!)

**Углубить знания:**
1. **HTTP методы и статус коды:**
   - GET, POST, PUT, PATCH, DELETE
   - 200, 201, 400, 401, 403, 404, 500
   
2. **Аутентификация:**
   - Basic Auth
   - Bearer Token (JWT)
   - OAuth 2.0
   - API Keys

3. **Продвинутые техники RestAssured:**
   - Работа с cookies и headers
   - Сериализация/десериализация
   - Валидация JSON Schema
   - Параметризованные тесты
   - Request/Response фильтры

4. **Особенности тестирования API:**
   - Тестирование граничных значений
   - Негативные тесты
   - Тестирование производительности
   - Тестирование безопасности

**Практика:**
- Улучшить существующие API тесты
- Добавить больше негативных сценариев
- Использовать JSON Schema валидацию

---

### 4. PLAYWRIGHT (Альтернатива Selenium)

**Что это:** Современный инструмент для E2E тестирования.

**Преимущества:**
- Быстрее Selenium
- Автоматическое ожидание
- Встроенные скриншоты/видео
- Поддержка нескольких браузеров

**Что изучить:**
1. Базовые команды
2. Page Object Model
3. Ожидания (waits)
4. Скриншоты и видео
5. Параллельный запуск

**Практика:**
- Переписать 1-2 UI теста на Playwright
- Сравнить с Selenium

**Ресурсы:**
- https://playwright.dev/java/ - Официальная документация Playwright для Java
- https://playwright.dev/java/docs/intro - Начало работы с Playwright Java
- https://playwright.dev/java/docs/test-assertions - Ассерты и проверки
- https://habr.com/ru/articles/ - Поиск статей на Habr по запросу "Playwright Java"
- https://www.baeldung.com/java-playwright - Руководство по Playwright на Baeldung

---

### 5. SQL (Базовые знания)

**Что повторить:**
1. **CRUD операции:**
   - SELECT, INSERT, UPDATE, DELETE
   
2. **JOIN:**
   - INNER JOIN
   - LEFT/RIGHT JOIN
   - FULL OUTER JOIN
   
3. **Полезные функции:**
   - WHERE, GROUP BY, HAVING
   - ORDER BY, LIMIT
   - Агрегатные функции (COUNT, SUM, AVG)

**Практика:**
- Написать SQL запросы для проверки данных в БД
- Использовать в тестах для подготовки/очистки данных

---

### 6. MONGODB (Желательно)

**Что это:** NoSQL документо-ориентированная БД.

**Что изучить:**
1. Основы MongoDB
2. CRUD операции
3. Работа через Java драйвер
4. Запросы (find, update, delete)
5. Агрегации

**Практика:**
- Подключиться к MongoDB
- Написать простые операции
- Использовать в тестах

**Ресурсы:**
- https://www.mongodb.com/docs/
- https://www.baeldung.com/java-mongodb
- См. также: `../comparisons/MongoDB_vs_Redis.md` для сравнения с Redis

---

### 7. KAFKA (Желательно)

**Что это:** Распределенная платформа для потоковой обработки данных.

**Что изучить:**
1. Основы Kafka (Producer, Consumer, Topics)
2. Работа с сообщениями
3. Тестирование Kafka приложений
4. Использование Testcontainers для Kafka

**Практика:**
- Настроить Kafka локально (Docker)
- Написать Producer/Consumer
- Протестировать отправку/получение сообщений

**Ресурсы:**
- https://kafka.apache.org/documentation/
- https://www.baeldung.com/kafka-testcontainers

---

### 8. KIBANA (Желательно)

**Что это:** Визуализация и анализ логов из Elasticsearch.

**Что изучить:**
1. Поиск в логах
2. Фильтры и запросы
3. Анализ ошибок
4. Дашборды

**Практика:**
- Настроить локально ELK стек (Elasticsearch, Logstash, Kibana)
- Искать ошибки в логах
- Создать простой дашборд

**Ресурсы:**
- https://www.elastic.co/guide/en/kibana/current/index.html

---

## 📚 Теоретическая подготовка

### Java Core (Уверенные знания!)

**Повторить:**
1. Коллекции (List, Set, Map)
2. Stream API
3. Многопоточность (Thread, ExecutorService)
4. Исключения (Exception handling)
5. Аннотации
6. Generics

### ООП (Понимание принципов!)

**Повторить:**
1. Инкапсуляция
2. Наследование
3. Полиморфизм
4. Абстракция

### Паттерны проектирования

**Обязательно знать:**
1. **Page Object Model** (для UI тестов)
2. **Builder Pattern** (для создания объектов)
3. **Factory Pattern** (для создания объектов)
4. **Singleton** (для утилитных классов)
5. **Strategy** (для разных стратегий тестирования)

**Практика:**
- Применить паттерны в существующих тестах
- Рефакторинг кода с использованием паттернов

---

## 🏗️ Архитектура фреймворков автоматизации

**Что понимать:**
1. **Слои тестового фреймворка:**
   - Test Layer (тесты)
   - Page/Service Layer (Page Objects, Services)
   - Core Layer (утилиты, конфигурация)
   - Data Layer (тестовые данные)

2. **Принципы:**
   - DRY (Don't Repeat Yourself)
   - SOLID принципы
   - Разделение ответственности
   - Переиспользование кода

3. **Best Practices:**
   - Именование тестов
   - Организация структуры проекта
   - Управление конфигурацией
   - Логирование
   - Обработка ошибок

---

## 🛠️ Инструменты и технологии

### Maven/Gradle
- Понимание структуры проекта
- Управление зависимостями
- Запуск тестов
- Плагины

### Chrome DevTools
**Что уметь:**
1. **Network tab:**
   - Анализ запросов/ответов
   - Фильтрация
   - Throttling (имитация медленного интернета)
   
2. **Console:**
   - Выполнение JavaScript
   - Логирование
   
3. **Elements:**
   - Инспектирование элементов
   - Поиск селекторов

---

## 📝 Практические задания для подготовки

### Задание 1: Контрактное тестирование с Pact
- Создать Consumer тест с Pact
- Создать Provider тест для верификации
- Настроить Pact Broker (опционально)

### Задание 2: Wiremock заглушки
- Настроить Wiremock
- Создать заглушки для внешнего API
- Использовать в тестах

### Задание 3: Улучшение API тестов
- Добавить больше негативных тестов
- Использовать JSON Schema валидацию
- Параметризовать тесты

### Задание 4: Playwright тест
- Переписать 1 UI тест на Playwright
- Сравнить производительность с Selenium

### Задание 5: Работа с БД
- Написать SQL запросы для подготовки данных
- Использовать в тестах для проверки состояния БД

### Задание 6: MongoDB клиент
- Подключиться к MongoDB
- Написать CRUD операции
- Использовать в тестах

---

## 🎤 Вопросы для собеседования (подготовка)

### Технические вопросы:

1. **Что такое контрактное тестирование?** (Контрактное тестирование - основная задача)
   - Объяснить концепцию
   - Разница между интеграционными и контрактными тестами
   - Когда использовать

2. **Как работает Wiremock?** (Wiremock - желательно)
   - Принцип работы
   - Когда использовать
   - Примеры использования

3. **Разница между Selenium и Playwright?** (Selenium/Playwright - уверенные знания)
   - Преимущества/недостатки
   - Когда что использовать

4. **Как тестировать API?** (Тестирование API - уверенные навыки!)
   - Подходы к тестированию
   - Что проверять
   - Инструменты

5. **Паттерны в автотестах?** (Паттерны проектирования автотестов!)
   - Page Object Model
   - Builder, Factory
   - Когда применять

6. **Как локализовать дефект в клиент-серверном приложении?** (Локализация дефектов в клиент-серверных приложениях)
   - Логи клиента/сервера
   - Network tab
   - Анализ запросов/ответов

---

## 📋 Полный список вопросов по автотестированию

### 🔵 Контрактное тестирование (Контрактное тестирование - основная задача!)

1. **Что такое контрактное тестирование и зачем оно нужно?**
   - Определение и концепция
   - Отличие от интеграционного тестирования
   - Преимущества и недостатки
   - Когда использовать контрактное тестирование

2. **Какие инструменты для контрактного тестирования вы знаете?**
   - Pact (Consumer-Driven Contracts)
   - Spring Cloud Contract
   - Сравнение инструментов
   - Когда что использовать

3. **Как работает Pact?**
   - Consumer и Provider
   - Pact Broker
   - Процесс верификации контрактов
   - Интеграция в CI/CD

4. **Что такое Pact Broker и зачем он нужен?**
   - Определение и назначение
   - Основные функции (хранение, верификация, управление зависимостями)
   - Как работает процесс публикации и верификации контрактов
   - Визуализация и веб-интерфейс
   - Интеграция с CI/CD
   - Альтернативы Pact Broker

5. **В чем разница между Consumer-Driven и Provider-Driven контрактами?**
   - Подходы к созданию контрактов
   - Преимущества каждого подхода
   - Практические примеры

6. **Как организовать контрактное тестирование в микросервисной архитектуре?**
   - Стратегия тестирования
   - Управление контрактами
   - Версионирование контрактов

---

### 🔵 REST API тестирование (REST - уверенные знания!, Тестирование API - уверенные навыки!)

6. **Что такое REST API?**
   - Принципы REST
   - Ресурсы, методы HTTP
   - Статус коды
   - Форматы данных (JSON, XML)

7. **Какие HTTP методы вы знаете и когда их использовать?**
   - GET, POST, PUT, PATCH, DELETE
   - OPTIONS, HEAD
   - Идемпотентность и безопасность методов
   - Примеры использования

8. **Какие HTTP статус коды вы знаете?**
   - 2xx (успех): 200, 201, 204
   - 4xx (ошибка клиента): 400, 401, 403, 404, 409
   - 5xx (ошибка сервера): 500, 502, 503
   - Когда какой код возвращать

9. **Как тестировать API?**
   - Позитивные и негативные тесты
   - Граничные значения
   - Валидация запросов и ответов
   - Тестирование производительности
   - Тестирование безопасности

10. **Какие инструменты для тестирования API вы знаете?**
    - RestAssured (Java)
    - Postman
    - SoapUI
    - HTTPie, curl
    - Сравнение инструментов

11. **Как работать с аутентификацией в API тестах?**
    - Basic Auth
    - Bearer Token (JWT)
    - OAuth 2.0
    - API Keys
    - Сессии и cookies

12. **Как валидировать ответы API?**
    - Проверка статус кода
    - Валидация структуры JSON
    - JSON Schema валидация
    - Проверка типов данных
    - Проверка обязательных полей

13. **Как тестировать API с параметрами?**
    - Query параметры
    - Path параметры
    - Headers
    - Body параметры
    - Параметризованные тесты

14. **Как обрабатывать ошибки в API тестах?**
    - Обработка исключений
    - Проверка сообщений об ошибках
    - Retry механизмы
    - Логирование ошибок

---

### 🔵 UI тестирование (Тестирование WEB, Selenium/Playwright - уверенные знания)

15. **Какие инструменты для UI тестирования вы знаете?**
    - Selenium WebDriver
    - Playwright
    - Cypress
    - Selenide
    - Сравнение инструментов

16. **В чем разница между Selenium и Playwright?**
    - Архитектура
    - Производительность
    - Автоматические ожидания
    - Поддержка браузеров
    - API и возможности

17. **Что такое Page Object Model (POM)?** (Паттерны проектирования автотестов!)
    - Концепция POM
    - Преимущества использования
    - Реализация в коде
    - Best practices

18. **Как работать с ожиданиями (waits) в UI тестах?**
    - Implicit waits
    - Explicit waits
    - Fluent waits
    - Ожидание по условиям
    - Ожидание в Playwright

19. **Как находить элементы на странице?**
    - CSS селекторы
    - XPath
    - ID, Name, Class
    - Data-атрибуты
    - Best practices для селекторов

20. **Как работать с iframe, popup, alerts?**
    - Переключение между окнами
    - Работа с iframe
    - Обработка alert/confirm/prompt
    - Модальные окна

21. **Как работать с файлами в UI тестах?**
    - Загрузка файлов
    - Скачивание файлов
    - Проверка содержимого файлов

22. **Как делать скриншоты и видео в тестах?**
    - Скриншоты при ошибках
    - Скриншоты элементов
    - Видео записи тестов
    - Интеграция с Allure

23. **Как тестировать мобильные приложения?**
    - Appium
    - Эмуляторы и симуляторы
    - Отличия от веб-тестирования

---

### 🔵 Регрессионное тестирование (Регрессионное тестирование - задача)

24. **Что такое регрессионное тестирование?**
    - Определение
    - Виды регрессии
    - Стратегии регрессионного тестирования
    - Полная vs частичная регрессия

25. **Как организовать регрессионное тестирование?**
    - Выбор тестов для регрессии
    - Приоритизация тестов
    - Автоматизация регрессии
    - Smoke тесты

26. **Соотношение ручного и автоматизированного тестирования 50/50 - как это организовать?**
    - Какие тесты автоматизировать
    - Какие тесты оставить ручными
    - Критерии выбора
    - Баланс между скоростью и покрытием

---

### 🔵 SQL и работа с БД (SQL - базовые знания, Клиенты взаимодействия с БД)

27. **Какие SQL операции вы знаете?** (SQL CRUD + join)
    - SELECT, INSERT, UPDATE, DELETE
    - CREATE, DROP, ALTER
    - Примеры запросов

28. **Что такое JOIN и какие виды JOIN вы знаете?** (SQL join)
    - INNER JOIN
    - LEFT JOIN / RIGHT JOIN
    - FULL OUTER JOIN
    - CROSS JOIN
    - Примеры использования

29. **Как использовать агрегатные функции?**
    - COUNT, SUM, AVG, MIN, MAX
    - GROUP BY
    - HAVING
    - Примеры запросов

30. **Как работать с БД в автотестах?**
    - Подключение к БД
    - Подготовка тестовых данных
    - Проверка данных после тестов
    - Очистка данных
    - Использование транзакций

31. **Как использовать клиенты взаимодействия с БД в автотестах?** (Клиенты взаимодействия с БД)
    - JDBC
    - ORM (Hibernate, JPA)
    - Создание клиентов для тестов
    - Интеграция с тестовым фреймворком

---

### 🔵 MongoDB (MongoDB - желательно)

32. **Что такое MongoDB и чем она отличается от реляционных БД?**
    - NoSQL концепция
    - Документо-ориентированная БД
    - Преимущества и недостатки
    - Когда использовать

33. **Как работать с MongoDB в Java?**
    - MongoDB Java Driver
    - CRUD операции
    - Запросы (find, update, delete)
    - Агрегации

34. **Как использовать MongoDB в автотестах?**
    - Подключение
    - Подготовка данных
    - Проверка данных
    - Очистка коллекций

---

### 🔵 Kafka (Kafka - желательно)

35. **Что такое Kafka и зачем он нужен?**
    - Брокер сообщений
    - Producer, Consumer, Topics
    - Преимущества использования
    - Use cases

36. **Как тестировать приложения с Kafka?**
    - Тестирование Producer
    - Тестирование Consumer
    - Использование Testcontainers
    - Мокирование Kafka

---

### 🔵 Wiremock (Wiremock - желательно)

37. **Что такое Wiremock и зачем он нужен?**
    - Мокирование HTTP сервисов
    - Изоляция тестов
    - Преимущества использования

38. **Как настроить и использовать Wiremock?**
    - Настройка сервера
    - Создание стабов
    - Запись/воспроизведение запросов
    - Динамические ответы
    - Интеграция с тестами

---

### 🔵 Kibana и логи (Kibana - желательно)

39. **Как работать с логами в тестировании?**
    - Поиск ошибок в логах
    - Анализ логов приложения
    - Использование Kibana
    - Фильтры и запросы
    - Дашборды

40. **Как использовать логи для локализации дефектов?** (Локализация дефектов)
    - Анализ логов клиента
    - Анализ логов сервера
    - Корреляция логов
    - Поиск по времени и контексту

---

### 🔵 Java Core (Java Core - уверенные знания!)

41. **Какие коллекции в Java вы знаете?**
    - List (ArrayList, LinkedList)
    - Set (HashSet, TreeSet)
    - Map (HashMap, TreeMap)
    - Когда что использовать

42. **Что такое Stream API?**
    - Потоки данных
    - Операции (map, filter, reduce)
    - Параллельные потоки
    - Примеры использования

43. **Как работает многопоточность в Java?**
    - Thread, Runnable
    - ExecutorService
    - Future, CompletableFuture
    - Синхронизация
    - Применение в тестах

44. **Как обрабатывать исключения?**
    - try-catch-finally
    - Checked vs Unchecked исключения
    - Custom исключения
    - Best practices

45. **Что такое Generics?**
    - Параметризованные типы
    - Wildcards
    - Ограничения типов
    - Примеры использования

46. **Что такое аннотации?**
    - Встроенные аннотации
    - Custom аннотации
    - Аннотации в тестах (JUnit, TestNG)
    - Reflection и аннотации

---

### 🔵 ООП (ООП - понимание основных принципов!)

47. **Какие принципы ООП вы знаете?**
    - Инкапсуляция
    - Наследование
    - Полиморфизм
    - Абстракция
    - Примеры применения

48. **Что такое инкапсуляция?**
    - Сокрытие реализации
    - Модификаторы доступа
    - Getters и Setters
    - Примеры в тестах

49. **Что такое наследование?**
    - Классы и подклассы
    - Переопределение методов
    - Ключевое слово super
    - Примеры использования

50. **Что такое полиморфизм?**
    - Перегрузка методов
    - Переопределение методов
    - Интерфейсы
    - Примеры применения

---

### 🔵 Паттерны проектирования (Паттерны проектирования ПО и автотестов!)

51. **Какие паттерны проектирования вы знаете?**
    - Creational (Builder, Factory, Singleton)
    - Structural (Adapter, Decorator)
    - Behavioral (Strategy, Observer)
    - Применение в автотестах

52. **Что такое Builder Pattern?**
    - Концепция
    - Реализация
    - Преимущества
    - Примеры в тестах (построение тестовых данных)

53. **Что такое Factory Pattern?**
    - Концепция
    - Виды Factory
    - Применение в тестах
    - Примеры использования

54. **Что такое Singleton Pattern?**
    - Концепция
    - Реализация
    - Применение в тестах (утилитные классы)
    - Потокобезопасность

55. **Что такое Strategy Pattern?**
    - Концепция
    - Применение в тестах
    - Примеры (разные стратегии тестирования)

56. **Какие паттерны специфичны для автотестов?**
    - Page Object Model
    - Page Factory
    - Fluent Interface
    - Test Data Builder
    - Object Mother

---

### 🔵 Архитектура фреймворков автоматизации (Архитектура фреймворков автоматизации!)

57. **Как организовать архитектуру тестового фреймворка?**
    - Слои тестового фреймворка
    - Разделение ответственности
    - Принципы организации
    - Best practices

58. **Какие слои должны быть в тестовом фреймворке?**
    - Test Layer (тесты)
    - Page/Service Layer (Page Objects, Services)
    - Core Layer (утилиты, конфигурация)
    - Data Layer (тестовые данные)
    - Infrastructure Layer

59. **Какие принципы применяются при построении фреймворка?**
    - DRY (Don't Repeat Yourself)
    - SOLID принципы
    - KISS (Keep It Simple, Stupid)
    - YAGNI (You Aren't Gonna Need It)

60. **Как организовать конфигурацию тестов?**
    - Файлы конфигурации
    - Переменные окружения
    - Параметры запуска
    - Управление окружениями

61. **Как организовать тестовые данные?**
    - Генерация данных
    - Хранение данных
    - Подготовка и очистка данных
    - Параметризация тестов

---

### 🔵 JUnit/TestNG (JUnit/TestNG - уверенные знания)

62. **Какие фреймворки для тестирования в Java вы знаете?**
    - JUnit 4, JUnit 5
    - TestNG
    - KTest (Kotlin)
    - Сравнение фреймворков

63. **Как организовать тесты в JUnit 5?**
    - Аннотации (@Test, @BeforeEach, @AfterEach)
    - Группировка тестов
    - Параметризованные тесты
    - Условное выполнение

64. **Как использовать TestNG?**
    - Аннотации
    - Группы тестов
    - Параметризация
    - Зависимости между тестами
    - Параллельный запуск

65. **Как организовать setup и teardown?**
    - @BeforeAll, @AfterAll
    - @BeforeEach, @AfterEach
    - @BeforeClass, @AfterClass
    - Best practices

66. **Как параметризовать тесты?**
    - @ParameterizedTest
    - @ValueSource, @CsvSource
    - @MethodSource
    - Динамические тесты

---

### 🔵 Maven/Gradle (Maven/Gradle - понимание систем сборки)

67. **Как работает Maven?**
    - Структура проекта
    - pom.xml
    - Жизненный цикл
    - Зависимости и плагины
    - Запуск тестов

68. **Как работает Gradle?**
    - Структура проекта
    - build.gradle
    - Задачи (tasks)
    - Зависимости
    - Запуск тестов

69. **Как управлять зависимостями?**
    - Добавление зависимостей
    - Версионирование
    - Разрешение конфликтов
    - Транзитивные зависимости

---

### 🔵 Chrome DevTools (Chrome DevTools - умение использовать)

70. **Как использовать панель разработчика в Chrome?**
    - Открытие DevTools
    - Основные вкладки
    - Горячие клавиши
    - Настройка

71. **Как работать с вкладкой Network?**
    - Анализ запросов/ответов
    - Фильтрация запросов
    - Throttling (имитация медленного интернета)
    - Сохранение HAR файлов
    - Анализ производительности

72. **Как работать с вкладкой Console?**
    - Выполнение JavaScript
    - Логирование
    - Отладка
    - Работа с объектами

73. **Как работать с вкладкой Elements?**
    - Инспектирование элементов
    - Поиск селекторов
    - Изменение стилей
    - Проверка верстки
    - Поиск элементов

---

### 🔵 Клиент-серверные приложения (Клиент-серверные приложения, локализация дефектов)

74. **Как работают клиент-серверные приложения?**
    - Архитектура
    - Протоколы (HTTP, HTTPS)
    - Request/Response цикл
    - Сессии и cookies

75. **Как локализовать дефект в клиент-серверном приложении?**
    - Анализ логов клиента
    - Анализ логов сервера
    - Network tab в DevTools
    - Анализ запросов/ответов
    - Проверка статус кодов
    - Анализ ошибок в консоли

76. **Как определить, где находится дефект (клиент или сервер)?**
    - Анализ HTTP запросов
    - Проверка валидности запроса
    - Анализ ответа сервера
    - Проверка логики на клиенте
    - Использование логов

---

### 🔵 Общие вопросы по автотестированию

77. **Что такое автоматизация тестирования?**
    - Определение
    - Преимущества и недостатки
    - Когда автоматизировать
    - ROI автоматизации

78. **Какие виды тестирования можно автоматизировать?**
    - Unit тесты
    - Integration тесты
    - E2E тесты
    - API тесты
    - Performance тесты

79. **Как выбрать тесты для автоматизации?**
    - Критерии выбора
    - Приоритизация
    - ROI анализ
    - Частота выполнения

80. **Как организовать CI/CD для автотестов?**
    - Интеграция в pipeline
    - Запуск тестов
    - Отчеты
    - Уведомления
    - Параллельный запуск

81. **Как поддерживать автотесты?**
    - Рефакторинг
    - Обновление селекторов
    - Управление версиями
    - Документация
    - Code review

82. **Как обрабатывать флаки (flaky) тесты?**
    - Причины флаки тестов
    - Стратегии устранения
    - Retry механизмы
    - Мониторинг стабильности

83. **Как измерять качество автотестов?**
    - Покрытие кода
    - Стабильность тестов
    - Скорость выполнения
    - Полезность тестов

84. **Как организовать отчетность по тестам?**
    - Allure отчеты
    - HTML отчеты
    - Интеграция с системами
    - Дашборды

---

## 💡 Примеры запросов для практики

### REST API запросы (REST - уверенные знания!)

**Пример 1: GET запрос с параметрами**
```http
GET /api/users?page=1&limit=10&status=active
Headers:
  Authorization: Bearer {token}
  Content-Type: application/json
```

**Пример 2: POST запрос с телом**
```http
POST /api/users
Headers:
  Authorization: Bearer {token}
  Content-Type: application/json
Body:
{
  "name": "John Doe",
  "email": "john@example.com",
  "age": 30
}
```

**Пример 3: PUT запрос для обновления**
```http
PUT /api/users/123
Headers:
  Authorization: Bearer {token}
  Content-Type: application/json
Body:
{
  "name": "John Smith",
  "email": "john.smith@example.com"
}
```

**Пример 4: DELETE запрос**
```http
DELETE /api/users/123
Headers:
  Authorization: Bearer {token}
```

**Пример 5: PATCH запрос для частичного обновления**
```http
PATCH /api/users/123
Headers:
  Authorization: Bearer {token}
  Content-Type: application/json
Body:
{
  "email": "newemail@example.com"
}
```

### SQL запросы (SQL CRUD + join)

**Пример 1: SELECT с JOIN**
```sql
SELECT u.id, u.name, u.email, o.order_id, o.total
FROM users u
INNER JOIN orders o ON u.id = o.user_id
WHERE u.status = 'active'
ORDER BY o.total DESC;
```

**Пример 2: INSERT**
```sql
INSERT INTO users (name, email, status)
VALUES ('John Doe', 'john@example.com', 'active');
```

**Пример 3: UPDATE**
```sql
UPDATE users
SET email = 'newemail@example.com', status = 'inactive'
WHERE id = 123;
```

**Пример 4: DELETE**
```sql
DELETE FROM users
WHERE status = 'inactive' AND created_at < '2024-01-01';
```

**Пример 5: SELECT с агрегацией и GROUP BY**
```sql
SELECT u.name, COUNT(o.order_id) as order_count, SUM(o.total) as total_spent
FROM users u
LEFT JOIN orders o ON u.id = o.user_id
GROUP BY u.id, u.name
HAVING COUNT(o.order_id) > 5
ORDER BY total_spent DESC;
```

**Пример 6: Сложный JOIN с несколькими таблицами**
```sql
SELECT u.name, p.product_name, oi.quantity, oi.price
FROM users u
INNER JOIN orders o ON u.id = o.user_id
INNER JOIN order_items oi ON o.order_id = oi.order_id
INNER JOIN products p ON oi.product_id = p.product_id
WHERE o.status = 'completed'
ORDER BY o.created_at DESC;
```

### MongoDB запросы (MongoDB - желательно)

**Пример 1: Find**
```javascript
db.users.find({ status: "active", age: { $gte: 18 } })
```

**Пример 2: Insert**
```javascript
db.users.insertOne({
  name: "John Doe",
  email: "john@example.com",
  age: 30,
  status: "active"
})
```

**Пример 3: Update**
```javascript
db.users.updateOne(
  { email: "john@example.com" },
  { $set: { status: "inactive" } }
)
```

**Пример 4: Delete**
```javascript
db.users.deleteOne({ email: "john@example.com" })
```

**Пример 5: Aggregation**
```javascript
db.orders.aggregate([
  { $match: { status: "completed" } },
  { $group: { _id: "$user_id", total: { $sum: "$amount" } } },
  { $sort: { total: -1 } }
])
```

### Wiremock примеры (Wiremock - желательно)

**Пример 1: Простой стаб**
```java
stubFor(get(urlEqualTo("/api/users/123"))
    .willReturn(aResponse()
        .withStatus(200)
        .withHeader("Content-Type", "application/json")
        .withBody("{\"id\": 123, \"name\": \"John Doe\"}")));
```

**Пример 2: Стаб с параметрами**
```java
stubFor(get(urlPathEqualTo("/api/users"))
    .withQueryParam("status", equalTo("active"))
    .willReturn(aResponse()
        .withStatus(200)
        .withBody("[{\"id\": 1, \"name\": \"User 1\"}]")));
```

**Пример 3: POST стаб**
```java
stubFor(post(urlEqualTo("/api/users"))
    .withRequestBody(equalToJson("{\"name\": \"John\", \"email\": \"john@example.com\"}"))
    .willReturn(aResponse()
        .withStatus(201)
        .withBody("{\"id\": 123, \"name\": \"John\"}")));
```

### RestAssured примеры (REST API - уверенные навыки!)

**Пример 1: GET запрос**
```java
given()
    .header("Authorization", "Bearer " + token)
    .queryParam("page", 1)
    .queryParam("limit", 10)
.when()
    .get("/api/users")
.then()
    .statusCode(200)
    .body("data.size()", equalTo(10))
    .body("data[0].name", notNullValue());
```

**Пример 2: POST запрос**
```java
User user = new User("John Doe", "john@example.com");

given()
    .header("Authorization", "Bearer " + token)
    .contentType(ContentType.JSON)
    .body(user)
.when()
    .post("/api/users")
.then()
    .statusCode(201)
    .body("id", notNullValue())
    .body("name", equalTo("John Doe"));
```

**Пример 3: Валидация JSON Schema**
```java
given()
    .header("Authorization", "Bearer " + token)
.when()
    .get("/api/users/123")
.then()
    .statusCode(200)
    .body(matchesJsonSchemaInClasspath("user-schema.json"));
```

### Контрактное тестирование - валидация Swagger/OpenAPI (Контрактное тестирование!)

**Пример 1: Валидация ответа против JSON Schema (из OpenAPI)**
```java
// Валидация структуры ответа против схемы
given()
    .header("Authorization", "Bearer " + token)
.when()
    .get("/api/users/123")
.then()
    .statusCode(200)
    .body(matchesJsonSchemaInClasspath("schemas/user-schema.json")); // Валидация схемы
```

**Пример 2: Проверка обязательных полей (соответствие OpenAPI)**
```java
// Проверка, что все обязательные поля из OpenAPI присутствуют
given()
    .header("Authorization", "Bearer " + token)
.when()
    .get("/api/users/123")
.then()
    .statusCode(200)
    .body("id", notNullValue())           // Обязательное поле
    .body("name", notNullValue())         // Обязательное поле
    .body("email", notNullValue())        // Обязательное поле
    .body("email", matchesRegex("^[A-Za-z0-9+_.-]+@(.+)$")); // Валидация формата
```

**Пример 3: Валидация типов данных (соответствие OpenAPI)**
```java
// Проверка типов данных согласно OpenAPI спецификации
given()
    .header("Authorization", "Bearer " + token)
.when()
    .get("/api/users/123")
.then()
    .statusCode(200)
    .body("id", instanceOf(Integer.class))    // id должен быть Integer
    .body("name", instanceOf(String.class))   // name должен быть String
    .body("age", instanceOf(Integer.class))   // age должен быть Integer
    .body("active", instanceOf(Boolean.class)); // active должен быть Boolean
```

**Пример 4: Валидация структуры вложенных объектов**
```java
// Проверка структуры сложного ответа
given()
    .header("Authorization", "Bearer " + token)
.when()
    .get("/api/users/123")
.then()
    .statusCode(200)
    .body("id", notNullValue())
    .body("address", notNullValue())           // Вложенный объект
    .body("address.city", notNullValue())     // Поле вложенного объекта
    .body("address.street", notNullValue())
    .body("roles", hasSize(greaterThan(0)));   // Массив должен быть не пустым
```

**Пример 5: Сравнение реального ответа с ожидаемой структурой из Swagger**
```java
// Получаем ответ и проверяем его структуру
Response response = given()
    .header("Authorization", "Bearer " + token)
.when()
    .get("/api/users/123")
.then()
    .statusCode(200)
    .extract().response();

// Проверяем, что структура соответствует OpenAPI спецификации
JsonPath jsonPath = response.jsonPath();
assertThat(jsonPath.get("id")).isNotNull();
assertThat(jsonPath.get("name")).isNotNull();
assertThat(jsonPath.get("email")).isNotNull();
assertThat(jsonPath.get("email")).asString().contains("@"); // Валидация email
```

**Пример 6: Валидация статус кодов (соответствие OpenAPI)**
```java
// Проверка, что статус коды соответствуют документации
// Согласно OpenAPI: GET /api/users/{id} возвращает 200 или 404

// Успешный случай
given()
    .header("Authorization", "Bearer " + token)
.when()
    .get("/api/users/123")
.then()
    .statusCode(200); // Согласно OpenAPI

// Несуществующий ресурс
given()
    .header("Authorization", "Bearer " + token)
.when()
    .get("/api/users/99999")
.then()
    .statusCode(404); // Согласно OpenAPI
```

### Playwright примеры (Playwright - уверенные знания)

**Пример 1: Базовый тест**
```java
@Test
public void testLogin() {
    page.navigate("https://example.com/login");
    page.fill("#username", "user");
    page.fill("#password", "pass");
    page.click("button[type='submit']");
    assertThat(page.locator(".welcome-message")).isVisible();
}
```

**Пример 2: Ожидание элемента**
```java
page.navigate("https://example.com");
page.waitForSelector(".dynamic-content");
String text = page.textContent(".dynamic-content");
assertThat(text).contains("Expected text");
```

**Пример 3: Работа с несколькими страницами**
```java
Page newPage = context.waitForPage(() -> {
    page.click("a[target='_blank']");
});
assertThat(newPage.url()).contains("expected-url");
```

---

## 📖 Готовые ответы для заучивания (ключевые вопросы)

### 1. Что такое контрактное тестирование? (Контрактное тестирование - основная задача!)

**Ответ:**

Контрактное тестирование — это проверка соответствия API реальной реализации документации (контракту). Контракт — это соглашение о том, как сервисы должны взаимодействовать.

**⚠️ ВАЖНО: Существует два основных подхода к контрактному тестированию!**

#### Подход 1: Валидация соответствия Swagger/OpenAPI (НАИБОЛЕЕ РАСПРОСТРАНЕННЫЙ!)

**Что это:**
- Swagger/OpenAPI — это спецификация (контракт), описывающая API
- Контрактное тестирование = проверка, что реальные запросы/ответы соответствуют этой спецификации
- Если API отклоняется от документации — тест падает

**Что проверяем:**
1. Структура запросов соответствует спецификации
2. Структура ответов соответствует спецификации
3. Обязательные поля присутствуют
4. Типы данных корректны
5. Статус коды соответствуют документации
6. Headers соответствуют спецификации

**Как это работает:**
- Получаем Swagger/OpenAPI спецификацию API
- В тестах валидируем реальные ответы против этой спецификации
- Используем JSON Schema валидацию
- Проверяем, что реальный API соответствует документации

**Пример:**
```java
// Валидация ответа против JSON Schema (из OpenAPI)
given()
    .header("Authorization", "Bearer " + token)
.when()
    .get("/api/users/123")
.then()
    .statusCode(200)
    .body(matchesJsonSchemaInClasspath("user-schema.json")); // Валидация схемы
```

**Инструменты:** RestAssured + JSON Schema, OpenAPI Validator, Schemathesis, Dredd, Postman

---

#### Подход 2: Pact / Consumer-Driven Contracts (ПРОДВИНУТЫЙ ПОДХОД)

**Что это:**
- Более продвинутый подход, где контракты создаются потребителем сервиса (consumer)
- Consumer определяет, как он ожидает взаимодействовать с провайдером (provider)
- Provider должен соответствовать этому контракту

**Ключевые моменты:**
- **Consumer-Driven Contracts (CDC)** — контракты создаются потребителем сервиса
- Контракт описывает формат запросов и ответов между сервисами
- Провайдер должен соответствовать контракту, иначе интеграция сломается
- Контракты хранятся в Pact Broker
- При изменении контракта провайдер получает уведомление

**Разница с интеграционным тестированием:**
- Интеграционные тесты проверяют реальное взаимодействие между сервисами (требуют запуска всех сервисов)
- Контрактные тесты проверяют только соответствие контракту (можно запускать изолированно)
- Контрактные тесты быстрее и стабильнее

**Когда использовать:**
- В микросервисной архитектуре
- Когда сервисы разрабатываются разными командами
- Для предотвращения breaking changes в API
- Для ускорения обратной связи при разработке

**Инструменты:** Pact, Spring Cloud Contract

---

#### Что такое Pact Broker?

**Pact Broker** — это веб-приложение для хранения и управления контрактами Pact между Consumer и Provider сервисами.

**Основные функции Pact Broker:**

1. **Хранение контрактов:**
   - Хранит все версии контрактов
   - История изменений контрактов
   - Версионирование контрактов

2. **Верификация контрактов:**
   - Provider может проверить, соответствует ли он контрактам всех Consumer
   - Автоматическая верификация при деплое Provider
   - Отчеты о верификации

3. **Управление зависимостями:**
   - Показывает, какие Consumer зависят от какого Provider
   - Отслеживание совместимости версий
   - Матрица совместимости

4. **Уведомления:**
   - Уведомления при изменении контрактов
   - Интеграция с CI/CD
   - Webhooks для автоматизации

5. **Визуализация:**
   - Веб-интерфейс для просмотра контрактов
   - Граф зависимостей между сервисами
   - Статус верификации контрактов

**Как работает Pact Broker:**

1. **Consumer создает контракт** в тестах и публикует его в Broker
2. **Broker хранит контракт** и его версию
3. **Provider верифицирует контракт** — запускает тесты против контракта
4. **Результаты верификации** публикуются обратно в Broker
5. **Broker показывает статус** — совместим ли Provider с контрактами Consumer

**Пример использования:**

```java
// Consumer публикует контракт в Broker
pact {
    publish {
        pactBrokerUrl = 'http://pact-broker:9292'
        version = '1.0.0'
    }
}

// Provider верифицирует контракты из Broker
@PactBroker(url = "http://pact-broker:9292")
public class UserServiceProviderTest {
    // Тесты верификации
}
```

**Альтернативы Pact Broker:**
- **PactFlow** — коммерческая версия с дополнительными возможностями
- **Локальное хранение** — контракты в файловой системе (для простых случаев)

---

**Какой подход использовать на собеседовании?**

**Рекомендуется знать ОБА подхода:**

1. **Начать с подхода 1 (Swagger/OpenAPI)** — это более распространенный подход
   - Большинство компаний используют именно этот подход
   - Проще объяснить на собеседовании
   - Легче реализовать

2. **Упомянуть про подход 2 (Pact)** — для демонстрации глубоких знаний
   - Показать, что знаете продвинутые техники
   - Полезно для микросервисной архитектуры

**Идеальный ответ:**
"Контрактное тестирование — это проверка соответствия API документации (например, Swagger/OpenAPI) реальной реализации. Мы проверяем, что запросы и ответы соответствуют спецификации. Также существует более продвинутый подход — Consumer-Driven Contracts с использованием Pact, где контракты создаются потребителем сервиса."

---

### 2. Как тестировать REST API? (REST - уверенные знания!, Тестирование API - уверенные навыки!)

**Ответ:**

Тестирование REST API включает проверку функциональности, производительности, безопасности и надежности API.

**Что проверять:**
1. **Позитивные тесты:**
   - Успешные запросы (200, 201 статусы)
   - Корректность структуры ответа
   - Валидность данных в ответе
   - Соответствие JSON Schema

2. **Негативные тесты:**
   - Невалидные данные (400 Bad Request)
   - Отсутствие аутентификации (401 Unauthorized)
   - Недостаточно прав (403 Forbidden)
   - Несуществующие ресурсы (404 Not Found)
   - Ошибки сервера (500 Internal Server Error)

3. **Граничные значения:**
   - Минимальные/максимальные значения
   - Пустые строки, null значения
   - Очень длинные строки
   - Специальные символы

4. **Безопасность:**
   - SQL injection
   - XSS атаки
   - Валидация входных данных
   - Проверка токенов и прав доступа

**Подходы:**
- **Black-box тестирование** — тестирование без знания внутренней реализации
- **White-box тестирование** — тестирование с пониманием логики
- **Пирамида тестирования** — больше unit тестов, меньше E2E тестов

**Инструменты:** RestAssured, Postman, SoapUI, HTTPie

---

### 3. В чем разница между Selenium и Playwright? (Selenium/Playwright - уверенные знания)

**Ответ:**

**Selenium:**
- Классический инструмент для автоматизации браузеров
- Работает через WebDriver протокол
- Требует явных ожиданий (waits)
- Медленнее Playwright
- Поддержка многих языков программирования
- Большое сообщество и документация

**Playwright:**
- Современный инструмент от Microsoft
- Работает напрямую с браузерами (без WebDriver)
- Автоматические ожидания элементов
- Быстрее Selenium (в 2-3 раза)
- Встроенная поддержка скриншотов и видео
- Автоматическое ожидание загрузки страницы
- Лучшая поддержка современных веб-технологий

**Когда использовать Selenium:**
- Когда нужна поддержка старых браузеров
- Когда команда уже знает Selenium
- Для простых проектов

**Когда использовать Playwright:**
- Для новых проектов
- Когда нужна высокая скорость выполнения
- Для сложных сценариев с ожиданиями
- Когда нужны встроенные скриншоты/видео

---

### 4. Что такое Page Object Model (POM)? (Паттерны проектирования автотестов!)

**Ответ:**

Page Object Model (POM) — это паттерн проектирования для организации кода автотестов, при котором каждая веб-страница представлена отдельным классом (Page Object), который инкапсулирует элементы страницы и методы для работы с ними.

**Преимущества:**
- **Переиспользование кода** — методы можно использовать в разных тестах
- **Поддержка** — изменения в UI требуют изменений только в одном месте
- **Читаемость** — тесты становятся более понятными
- **Разделение ответственности** — тесты не знают о деталях реализации страницы

**Структура:**
```java
public class LoginPage {
    private WebDriver driver;
    
    // Элементы страницы
    private By usernameField = By.id("username");
    private By passwordField = By.id("password");
    private By submitButton = By.id("submit");
    
    // Методы для работы со страницей
    public void enterUsername(String username) {
        driver.findElement(usernameField).sendKeys(username);
    }
    
    public void enterPassword(String password) {
        driver.findElement(passwordField).sendKeys(password);
    }
    
    public void clickSubmit() {
        driver.findElement(submitButton).click();
    }
    
    // Комплексные действия
    public void login(String username, String password) {
        enterUsername(username);
        enterPassword(password);
        clickSubmit();
    }
}
```

**Использование в тестах:**
```java
@Test
public void testLogin() {
    LoginPage loginPage = new LoginPage(driver);
    loginPage.login("user", "pass");
    // проверки
}
```

---

### 5. Как локализовать дефект в клиент-серверном приложении? (Локализация дефектов)

**Ответ:**

Локализация дефекта — это процесс определения, где именно находится проблема: на клиенте, на сервере, в сети или в базе данных.

**Шаги локализации:**

1. **Анализ Network tab в Chrome DevTools:**
   - Открыть DevTools (F12) → вкладка Network
   - Воспроизвести проблему
   - Проверить HTTP запросы:
     - Статус код ответа (200, 400, 500)
     - URL запроса
     - Headers запроса и ответа
     - Body запроса и ответа
   - Если запрос не отправлен → проблема на клиенте
   - Если запрос отправлен, но статус 4xx → проблема в запросе (клиент)
   - Если статус 5xx → проблема на сервере

2. **Анализ Console в DevTools:**
   - Проверить ошибки JavaScript
   - Проверить логи приложения
   - Ошибки в консоли указывают на проблемы клиента

3. **Анализ логов сервера:**
   - Проверить логи приложения на сервере
   - Использовать Kibana для поиска ошибок
   - Найти запрос по времени и контексту
   - Проверить стектрейсы ошибок

4. **Проверка базы данных:**
   - Выполнить SQL запросы для проверки данных
   - Проверить, сохранились ли данные
   - Проверить целостность данных

5. **Анализ запроса/ответа:**
   - Валидность JSON/XML структуры
   - Правильность данных в запросе
   - Корректность обработки ответа на клиенте

**Пример:**
Если пользователь не может залогиниться:
1. Проверить Network → запрос POST /api/login отправлен?
2. Если да → какой статус? 401 → неверные credentials (клиент), 500 → ошибка сервера
3. Проверить логи сервера → найти запрос по времени
4. Проверить Console → есть ли ошибки JavaScript?

---

### 6. Какие принципы ООП вы знаете? (ООП - понимание основных принципов!)

**Ответ:**

ООП (Объектно-Ориентированное Программирование) базируется на четырех основных принципах:

**1. Инкапсуляция:**
- Сокрытие внутренней реализации объекта
- Предоставление публичного интерфейса для взаимодействия
- Использование модификаторов доступа (private, protected, public)
- Пример: класс с приватными полями и публичными методами getter/setter

**2. Наследование:**
- Возможность создания нового класса на основе существующего
- Переиспользование кода
- Ключевое слово `extends` в Java
- Пример: класс `BasePage` с общими методами, от него наследуются `LoginPage`, `HomePage`

**3. Полиморфизм:**
- Возможность объектов разных классов обрабатываться через единый интерфейс
- Перегрузка методов (overloading) — методы с одинаковым именем, но разными параметрами
- Переопределение методов (overriding) — изменение поведения метода в подклассе
- Пример: интерфейс `WebDriver`, реализуемый `ChromeDriver`, `FirefoxDriver`

**4. Абстракция:**
- Выделение существенных характеристик объекта
- Сокрытие деталей реализации
- Использование абстрактных классов и интерфейсов
- Пример: интерфейс `Page` с абстрактными методами, конкретные страницы реализуют его

**Применение в автотестах:**
- Page Objects используют инкапсуляцию
- Базовые классы используют наследование
- Интерфейсы для разных драйверов используют полиморфизм
- Абстракция помогает создавать гибкую архитектуру

---

### 7. Что такое Builder Pattern? (Паттерны проектирования автотестов!)

**Ответ:**

Builder Pattern — это порождающий паттерн проектирования, который позволяет создавать сложные объекты пошагово. Паттерн позволяет создавать разные представления объекта, используя один и тот же процесс построения.

**Преимущества:**
- Улучшает читаемость кода
- Позволяет создавать объекты с множеством параметров
- Делает код более гибким
- Избегает конструкторов с множеством параметров

**Пример в автотестах:**

```java
// Класс для тестовых данных
public class User {
    private String name;
    private String email;
    private int age;
    private String status;
    
    private User(UserBuilder builder) {
        this.name = builder.name;
        this.email = builder.email;
        this.age = builder.age;
        this.status = builder.status;
    }
    
    public static class UserBuilder {
        private String name;
        private String email;
        private int age;
        private String status = "active"; // значение по умолчанию
        
        public UserBuilder name(String name) {
            this.name = name;
            return this;
        }
        
        public UserBuilder email(String email) {
            this.email = email;
            return this;
        }
        
        public UserBuilder age(int age) {
            this.age = age;
            return this;
        }
        
        public UserBuilder status(String status) {
            this.status = status;
            return this;
        }
        
        public User build() {
            return new User(this);
        }
    }
}

// Использование
User user = new User.UserBuilder()
    .name("John Doe")
    .email("john@example.com")
    .age(30)
    .status("active")
    .build();
```

**Применение в тестах:**
- Создание тестовых данных
- Построение сложных объектов запросов
- Конфигурация тестовых окружений

---

### 8. Как организовать архитектуру тестового фреймворка? (Архитектура фреймворков автоматизации!)

**Ответ:**

Архитектура тестового фреймворка должна быть организована по слоям с четким разделением ответственности.

**Слои фреймворка:**

1. **Test Layer (Слой тестов):**
   - Содержит сами тестовые методы
   - Использует аннотации JUnit/TestNG
   - Содержит assertions (проверки)
   - Минимальная бизнес-логика

2. **Page/Service Layer (Слой страниц/сервисов):**
   - Page Objects для UI тестов
   - Service Objects для API тестов
   - Инкапсулирует взаимодействие с приложением
   - Содержит методы для работы с элементами/эндпоинтами

3. **Core Layer (Ядро фреймворка):**
   - Утилитные классы
   - Конфигурация
   - Инициализация драйверов
   - Обработка исключений
   - Логирование

4. **Data Layer (Слой данных):**
   - Тестовые данные
   - Генерация данных
   - Работа с БД
   - Файлы с данными (JSON, CSV, Excel)

5. **Infrastructure Layer (Инфраструктурный слой):**
   - Настройка окружений
   - Docker контейнеры
   - CI/CD интеграция
   - Отчетность

**Принципы организации:**

- **DRY (Don't Repeat Yourself)** — избегать дублирования кода
- **SOLID принципы:**
  - Single Responsibility — один класс, одна ответственность
  - Open/Closed — открыт для расширения, закрыт для модификации
  - Liskov Substitution — подклассы должны заменять базовые классы
  - Interface Segregation — много специфичных интерфейсов лучше одного общего
  - Dependency Inversion — зависимость от абстракций, а не от конкретных классов
- **KISS (Keep It Simple, Stupid)** — простота важнее сложности
- **YAGNI (You Aren't Gonna Need It)** — не добавлять функциональность "на будущее"

**Структура проекта:**
```
src/test/java/
├── tests/              # Test Layer
├── pages/              # Page Layer
├── services/           # Service Layer
├── core/               # Core Layer
│   ├── config/
│   ├── utils/
│   └── drivers/
├── data/               # Data Layer
└── infrastructure/    # Infrastructure Layer
```

---

### 9. Как работает Wiremock? (Wiremock - желательно)

**Ответ:**

Wiremock — это инструмент для мокирования HTTP сервисов, который позволяет создавать стабы (заглушки) для внешних API и тестировать приложение в изоляции.

**Принцип работы:**
1. Запускается локальный HTTP сервер (Wiremock server)
2. Настраиваются стабы — правила, которые определяют, какой ответ вернуть на определенный запрос
3. Тесты отправляют запросы на Wiremock вместо реального API
4. Wiremock возвращает заранее определенные ответы

**Преимущества:**
- Изоляция тестов от внешних зависимостей
- Быстрое выполнение тестов (не нужно ждать реальные API)
- Предсказуемость (всегда известный ответ)
- Возможность тестировать различные сценарии (ошибки, задержки)

**Основные возможности:**
- Статические ответы
- Динамические ответы (на основе запроса)
- Запись реальных запросов/ответов
- Воспроизведение записанных ответов
- Задержки и таймауты
- Проверка запросов (верификация)

**Пример использования:**
```java
// Настройка стаба
stubFor(get(urlEqualTo("/api/users/123"))
    .willReturn(aResponse()
        .withStatus(200)
        .withHeader("Content-Type", "application/json")
        .withBody("{\"id\": 123, \"name\": \"John Doe\"}")));

// В тесте
Response response = given()
    .baseUri("http://localhost:8089") // Wiremock порт
    .when()
    .get("/api/users/123")
    .then()
    .statusCode(200)
    .extract().response();
```

**Когда использовать:**
- Тестирование интеграций с внешними API
- Когда внешний сервис недоступен
- Для тестирования различных сценариев ответов
- Для ускорения тестов

---

### 10. Что такое REST API? (REST - уверенные знания!)

**Ответ:**

REST (Representational State Transfer) — это архитектурный стиль для проектирования веб-сервисов, основанный на принципах HTTP протокола.

**Основные принципы REST:**

1. **Ресурсы (Resources):**
   - Все данные представлены как ресурсы
   - Ресурс идентифицируется URI (например: `/api/users/123`)
   - Ресурсы могут быть в разных форматах (JSON, XML)

2. **HTTP методы:**
   - **GET** — получение ресурса (идемпотентный, безопасный)
   - **POST** — создание нового ресурса
   - **PUT** — полное обновление ресурса (идемпотентный)
   - **PATCH** — частичное обновление ресурса
   - **DELETE** — удаление ресурса (идемпотентный)

3. **Статус коды:**
   - **2xx** — успех (200 OK, 201 Created, 204 No Content)
   - **4xx** — ошибка клиента (400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found)
   - **5xx** — ошибка сервера (500 Internal Server Error, 502 Bad Gateway, 503 Service Unavailable)

4. **Stateless (Без состояния):**
   - Каждый запрос содержит всю необходимую информацию
   - Сервер не хранит состояние клиента между запросами
   - Аутентификация через токены (JWT, Bearer Token)

5. **Единый интерфейс:**
   - Стандартизированные методы HTTP
   - Стандартные форматы данных (JSON, XML)

**Примеры REST API:**

```
GET    /api/users          # Получить список пользователей
GET    /api/users/123      # Получить пользователя с ID 123
POST   /api/users          # Создать нового пользователя
PUT    /api/users/123      # Обновить пользователя с ID 123
PATCH  /api/users/123      # Частично обновить пользователя
DELETE /api/users/123      # Удалить пользователя с ID 123
```

**Преимущества REST:**
- Простота использования
- Масштабируемость
- Кэширование
- Независимость от платформы

---

### 11. Как работать с SQL в автотестах? (SQL CRUD + join)

**Ответ:**

SQL используется в автотестах для подготовки данных, проверки результатов тестов и очистки данных после тестов.

**Основные операции (CRUD):**

1. **SELECT (Чтение):**
```sql
-- Простой запрос
SELECT * FROM users WHERE status = 'active';

-- С условиями
SELECT id, name, email FROM users 
WHERE age > 18 AND status = 'active'
ORDER BY name;

-- С агрегацией
SELECT status, COUNT(*) as count 
FROM users 
GROUP BY status;
```

2. **INSERT (Создание):**
```sql
-- Вставка одной записи
INSERT INTO users (name, email, status) 
VALUES ('John Doe', 'john@example.com', 'active');

-- Вставка нескольких записей
INSERT INTO users (name, email, status) 
VALUES 
    ('John Doe', 'john@example.com', 'active'),
    ('Jane Smith', 'jane@example.com', 'active');
```

3. **UPDATE (Обновление):**
```sql
-- Обновление одной записи
UPDATE users 
SET status = 'inactive', email = 'newemail@example.com'
WHERE id = 123;

-- Обновление нескольких записей
UPDATE users 
SET status = 'inactive'
WHERE created_at < '2024-01-01';
```

4. **DELETE (Удаление):**
```sql
-- Удаление одной записи
DELETE FROM users WHERE id = 123;

-- Удаление с условием
DELETE FROM users WHERE status = 'inactive' AND last_login < '2024-01-01';
```

**JOIN операции:**

1. **INNER JOIN** — возвращает только совпадающие записи:
```sql
SELECT u.name, o.order_id, o.total
FROM users u
INNER JOIN orders o ON u.id = o.user_id
WHERE u.status = 'active';
```

2. **LEFT JOIN** — возвращает все записи из левой таблицы:
```sql
SELECT u.name, o.order_id, o.total
FROM users u
LEFT JOIN orders o ON u.id = o.user_id;
```

3. **RIGHT JOIN** — возвращает все записи из правой таблицы:
```sql
SELECT u.name, o.order_id, o.total
FROM users u
RIGHT JOIN orders o ON u.id = o.user_id;
```

4. **FULL OUTER JOIN** — возвращает все записи из обеих таблиц:
```sql
SELECT u.name, o.order_id, o.total
FROM users u
FULL OUTER JOIN orders o ON u.id = o.user_id;
```

**Использование в тестах:**

```java
// Подготовка данных перед тестом
@BeforeEach
public void setUp() {
    jdbcTemplate.update(
        "INSERT INTO users (name, email) VALUES (?, ?)",
        "Test User", "test@example.com"
    );
}

// Проверка данных после теста
@Test
public void testUserCreation() {
    // выполнение действия
    createUserViaAPI();
    
    // проверка в БД
    User user = jdbcTemplate.queryForObject(
        "SELECT * FROM users WHERE email = ?",
        new Object[]{"test@example.com"},
        userRowMapper
    );
    assertThat(user).isNotNull();
    assertThat(user.getName()).isEqualTo("Test User");
}

// Очистка данных после теста
@AfterEach
public void tearDown() {
    jdbcTemplate.update("DELETE FROM users WHERE email = ?", "test@example.com");
}
```

---

### 12. Как использовать Chrome DevTools для тестирования? (Chrome DevTools)

**Ответ:**

Chrome DevTools — это набор инструментов разработчика в браузере Chrome, который помогает в тестировании и отладке веб-приложений.

**Основные вкладки:**

1. **Network (Сеть):**
   - Просмотр всех HTTP запросов и ответов
   - Анализ времени загрузки
   - Фильтрация запросов (по типу, статусу, домену)
   - Просмотр headers, cookies, body запросов/ответов
   - Сохранение HAR файлов
   - Throttling (имитация медленного интернета)
   - **Использование:** проверка API запросов, анализ производительности, отладка проблем с загрузкой

2. **Console (Консоль):**
   - Выполнение JavaScript кода
   - Просмотр логов приложения (console.log, console.error)
   - Отладка JavaScript ошибок
   - Работа с объектами страницы
   - **Использование:** отладка JavaScript, проверка переменных, выполнение команд

3. **Elements (Элементы):**
   - Инспектирование HTML структуры
   - Просмотр и изменение CSS стилей
   - Поиск элементов по селекторам
   - Проверка computed styles
   - **Использование:** поиск селекторов для автотестов, проверка верстки, отладка стилей

4. **Application (Приложение):**
   - Просмотр Local Storage, Session Storage
   - Просмотр Cookies
   - Просмотр IndexedDB
   - **Использование:** проверка хранения данных, очистка storage для тестов

5. **Performance (Производительность):**
   - Запись производительности страницы
   - Анализ времени загрузки
   - **Использование:** оптимизация производительности

**Практические примеры использования:**

**Пример 1: Анализ API запроса**
1. Открыть DevTools (F12)
2. Перейти на вкладку Network
3. Воспроизвести действие (например, клик на кнопку)
4. Найти нужный запрос в списке
5. Проверить:
   - URL запроса
   - Метод (GET, POST)
   - Headers (Authorization, Content-Type)
   - Request payload
   - Response (статус, body)
   - Время выполнения

**Пример 2: Поиск селектора для автотеста**
1. Открыть DevTools (F12)
2. Нажать Ctrl+Shift+C (или кликнуть на иконку выбора элемента)
3. Навести на нужный элемент на странице
4. В Elements найти выделенный элемент
5. Правый клик → Copy → Copy selector
6. Использовать скопированный селектор в тесте

**Пример 3: Проверка ошибок JavaScript**
1. Открыть DevTools (F12)
2. Перейти на вкладку Console
3. Воспроизвести проблему
4. Проверить красные ошибки в консоли
5. Кликнуть на ошибку для просмотра стека вызовов
6. Проверить файл и строку с ошибкой

**Пример 4: Имитация медленного интернета**
1. Открыть DevTools (F12)
2. Перейти на вкладку Network
3. В выпадающем списке выбрать "Slow 3G" или "Fast 3G"
4. Воспроизвести действие
5. Проверить поведение приложения при медленном соединении

---

### 13. Что такое регрессионное тестирование? (Регрессионное тестирование)

**Ответ:**

Регрессионное тестирование — это тип тестирования, который проверяет, что ранее работающий функционал продолжает работать после изменений в коде (новые функции, исправления багов, рефакторинг).

**Цель:**
- Убедиться, что новые изменения не сломали существующий функционал
- Обнаружить регрессии (ухудшения) в работе приложения

**Виды регрессии:**
1. **Локальная регрессия** — баг в измененном функционале
2. **Отдаленная регрессия** — баг в функционале, который не изменялся, но связан с измененным
3. **Незапланированная регрессия** — баг в функционале, который не должен был затронуть изменения

**Стратегии регрессионного тестирования:**

1. **Полная регрессия:**
   - Запуск всех тестов
   - Используется перед релизом
   - Долго выполняется, но дает максимальное покрытие

2. **Частичная регрессия:**
   - Запуск тестов, связанных с измененным функционалом
   - Быстрее выполняется
   - Меньше покрытие

3. **Приоритизированная регрессия:**
   - Запуск критичных тестов
   - Тесты по приоритетам (P0, P1, P2)
   - Баланс между скоростью и покрытием

**Организация регрессии при соотношении 50/50 (ручное/авто):**

- **Автоматизировать:**
  - Критичные сценарии (smoke тесты)
  - Часто выполняемые тесты
  - Стабильные тесты
  - API тесты
  - Регрессия базового функционала

- **Оставить ручными:**
  - Новый функционал (еще не автоматизирован)
  - Сложные сценарии с множеством шагов
  - Визуальное тестирование
  - Тестирование на разных устройствах/браузерах
  - Исследовательское тестирование

**Best practices:**
- Регулярный запуск регрессии (после каждого коммита, перед релизом)
- Поддержка актуальности тестов
- Приоритизация тестов
- Мониторинг стабильности тестов

---

### 14. Какие паттерны проектирования используются в автотестах? (Паттерны проектирования автотестов!)

**Ответ:**

В автотестах используются как общие паттерны проектирования, так и специфичные для тестирования.

**Общие паттерны:**

1. **Builder Pattern:**
   - Создание сложных объектов пошагово
   - Использование: построение тестовых данных, конфигурация объектов

2. **Factory Pattern:**
   - Создание объектов без указания конкретных классов
   - Использование: создание драйверов (ChromeDriverFactory, FirefoxDriverFactory)

3. **Singleton Pattern:**
   - Гарантия единственного экземпляра класса
   - Использование: утилитные классы, менеджеры драйверов

4. **Strategy Pattern:**
   - Выбор алгоритма во время выполнения
   - Использование: разные стратегии тестирования, разные способы аутентификации

**Специфичные паттерны для автотестов:**

1. **Page Object Model (POM):**
   - Каждая страница представлена классом
   - Инкапсуляция элементов и методов страницы
   - Самым популярный паттерн для UI тестов

2. **Page Factory:**
   - Расширение POM
   - Использование аннотаций @FindBy для инициализации элементов
   - Ленивая инициализация элементов

3. **Fluent Interface:**
   - Цепочка методов для улучшения читаемости
   - Пример: `page.login().withUsername("user").withPassword("pass").submit()`

4. **Test Data Builder:**
   - Построение тестовых данных через Builder
   - Гибкость в создании данных для разных сценариев

5. **Object Mother:**
   - Фабрика для создания тестовых объектов с предустановленными значениями
   - Упрощение создания тестовых данных

6. **Screenplay Pattern:**
   - Актеры выполняют задачи
   - Более читаемый и поддерживаемый код
   - Пример: `actor.attemptsTo(Login.with("user", "pass"))`

**Примеры применения:**

**Page Object Model:**
```java
public class LoginPage {
    private WebDriver driver;
    private By usernameField = By.id("username");
    
    public void enterUsername(String username) {
        driver.findElement(usernameField).sendKeys(username);
    }
}
```

**Factory Pattern:**
```java
public class WebDriverFactory {
    public static WebDriver createDriver(String browser) {
        switch(browser) {
            case "chrome": return new ChromeDriver();
            case "firefox": return new FirefoxDriver();
            default: throw new IllegalArgumentException();
        }
    }
}
```

**Builder Pattern:**
```java
User user = new UserBuilder()
    .withName("John")
    .withEmail("john@example.com")
    .build();
```

---

## 📅 Рекомендуемый график подготовки (2-3 недели)

### Неделя 1:
- День 1-2: Контрактное тестирование (Pact)
- День 3-4: Wiremock
- День 5: Повторение REST API, SQL

### Неделя 2:
- День 1-2: Playwright
- День 3: MongoDB
- День 4: Kafka
- День 5: Kibana, повторение

### Неделя 3:
- День 1-2: Практика (решение задач)
- День 3: Повторение теории
- День 4-5: Подготовка к собеседованию

---

## 🔗 Полезные ресурсы

### Контрактное тестирование:
- https://docs.pact.io/
- https://martinfowler.com/articles/consumerDrivenContracts.html

### API тестирование:
- https://rest-assured.io/
- https://www.postman.com/learn/

### Playwright:
- https://playwright.dev/java/

### Wiremock:
- http://wiremock.org/

### MongoDB:
- https://www.mongodb.com/docs/

### Kafka:
- https://kafka.apache.org/documentation/

---

## ✅ Чек-лист готовности

- [ ] Понимаю концепцию контрактного тестирования
- [ ] Могу настроить и использовать Wiremock
- [ ] Уверенно работаю с RestAssured
- [ ] Знаю разницу между Selenium и Playwright
- [ ] Могу написать SQL запросы с JOIN
- [ ] Понимаю основы MongoDB
- [ ] Знаю основные паттерны проектирования
- [ ] Понимаю архитектуру тестового фреймворка
- [ ] Умею использовать Chrome DevTools
- [ ] Могу локализовать дефект в клиент-серверном приложении

---

**Удачи на собеседовании! 🚀**
